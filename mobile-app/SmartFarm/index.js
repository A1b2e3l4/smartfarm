/**
 * SmartFarm Mobile App - Entry Point
 * 
 * A React Native application for connecting farmers with buyers.
 * Built with Expo, React Navigation, and Axios.
 */

import { registerRootComponent } from 'expo';
import App from './src/App';

// Register the main component
registerRootComponent(App);
